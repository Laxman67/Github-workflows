name: Greeting-App

on: [push, workflow_dispatch]

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Greet user
        run: echo "Hello, ${{ github.actor }}! Welcome to the Greeting-App."
